# SQLX

A Rust command-line database client for agents. SQLX manages encrypted user-level datasources and executes SQL through independently downloaded native and JDBC workers.

Initial database targets: MySQL, PostgreSQL, Oracle, and SQL Server.

Implementation is in progress. No binary release is available yet.
